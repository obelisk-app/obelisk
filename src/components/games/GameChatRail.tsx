// Deprecated: the game chat rail now reuses MessageArea/MessageInput
// directly from chat/page.tsx (same components + features as the voice
// chat rail). This file is a stub so any stale Turbopack client chunk
// that still imports it can resolve.
export default function GameChatRail(): null {
  return null;
}
